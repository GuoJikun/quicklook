# QuickLook Environment Variables Example
# Copy this file to .env and modify values as needed

# vcpkg Configuration
# Path to your vcpkg installation directory
# Windows example: VCPKG_ROOT=C:\vcpkg
# Linux/macOS example: VCPKG_ROOT=/opt/vcpkg
VCPKG_ROOT=

# Optional: vcpkg binary caching
# Enables faster builds by caching compiled packages
# Windows example: VCPKG_BINARY_SOURCES=files,C:\vcpkg-cache,readwrite
# Linux/macOS example: VCPKG_BINARY_SOURCES=files,/home/user/.vcpkg-cache,readwrite
VCPKG_BINARY_SOURCES=

# Optional: vcpkg feature flags
# VCPKG_FEATURE_FLAGS=manifests,versions,binarycaching

# Optional: Override default triplet
# VCPKG_DEFAULT_TRIPLET=x64-windows-static-md

# Build Configuration
# Set to 1 to enable debug logging during build
# DEBUG_BUILD=0

# Rust/Cargo Configuration  
# CARGO_TARGET_DIR=target

# Development Tools
# Optional: Path to custom tools
# LLVM_PATH=
# CMAKE_PATH=